process {
    publishDir = {"./results/$sid/$task.process"}
    scratch = true
    stageInMode = 'copy'
    stageOutMode = 'rsync'
    tag = { "$sid" }
}

params {
    output_dir="./results/"
    nb_threads=1
    para_diff=1.5e-3
    iso_diff=3e-3
    memory_limit='4 GB'
    b_thr=40
}
